# For NVIDIA V100 / Volta GPUs (compute capability 7.0).
# Install this when the cloud image ships a newer PyTorch wheel that dropped sm_70 support.
--index-url https://download.pytorch.org/whl/cu121
--extra-index-url https://pypi.org/simple

torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
pytorch-lightning>=2.0
PyYAML>=6.0
tqdm>=4.66
wandb>=0.16
tensorboard>=2.16
scikit-learn>=1.4
matplotlib>=3.8
